services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: crew_handshake
      POSTGRES_USER: crew
      POSTGRES_PASSWORD: crew
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data: {}
