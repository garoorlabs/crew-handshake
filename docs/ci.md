# CI

The repo uses path-based workflows:

- Frontend workflow runs when `frontend/**` changes.
- Backend workflow runs when `backend/**` changes.

Each workflow runs lint, tests, and build for its app.
