# Design system usage

UI styles must use design tokens defined in `frontend/src/styles/tokens.css`.
Avoid ad-hoc colors, spacing, or typography values in component styles.

Shared primitives live under `frontend/src/app/shared/ui/`.
